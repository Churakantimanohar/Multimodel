############################################################
# Core requirements (excluding PyTorch for manual install)
# On macOS (especially Apple Silicon), install PyTorch separately:
#   CPU/MPS (Python 3.13 compatible as of late 2025):
#   pip install torch==2.9.1 torchvision==0.24.1 torchaudio==2.9.1 --index-url https://download.pytorch.org/whl/cpu
#   (If newer versions appear, prefer matching trio: torch X.Y.Z -> torchvision 0.(X+15?).Z -> torchaudio X.Y.Z)
# or (if CUDA-capable external GPU): adjust index-url accordingly.
# We exclude torch here to avoid build failures.
############################################################
transformers>=4.40.0
numpy>=1.24.0,<2.0.0
pandas>=2.0.0
scikit-learn>=1.3.0
librosa>=0.10.1
soundfile>=0.12.1
opencv-python>=4.9.0
# mediapipe unavailable for Python 3.13 wheels; using fallback in code.
protobuf>=3.20.0,<4.0.0
streamlit>=1.32.0
streamlit-webrtc>=0.45.0
flask>=3.0.0
pydantic>=2.6.0
matplotlib>=3.8.0
seaborn>=0.13.0
pyyaml>=6.0.1
tqdm>=4.66.0
altair>=5.0.0
# pyaudio (optional for real-time mic) requires system portaudio headers
# Install with: brew install portaudio && pip install pyaudio
accelerate>=0.26.0
sentencepiece>=0.1.99
pydub>=0.25.1
